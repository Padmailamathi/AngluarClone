import { DropDownOpenDirective } from './drop-down-open.directive';

describe('DropDownOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownOpenDirective();
    expect(directive).toBeTruthy();
  });
});
