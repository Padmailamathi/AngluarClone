import { IsDropDownOpenDirective } from './is-drop-down-open.directive';

describe('IsDropDownOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDropDownOpenDirective();
    expect(directive).toBeTruthy();
  });
});
